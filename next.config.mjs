/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'assets.aceternity.com','plus.unsplash.com','i.pinimg.com'],
      },
};

export default nextConfig;
